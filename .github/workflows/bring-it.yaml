name: bring-it

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  # test:
  #   strategy:
  #     matrix:
  #       os:
  #         - macos-latest
  #         - windows-latest
  #         - ubuntu-latest
  #       node:
  #         - lts/*
  #         - lts/-1
  #       exclude:
  #         - os: ubuntu-latest
  #           node: lts/*

  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - name: Run
  #       uses: airkro/bring-it@actions
  #       with:
  #         node-version: ${{ matrix.node }}

  publish:
    # needs: [test]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: Run
        uses: airkro/bring-it@actions-oidc
        with:
          custom-command-before: |
            pnpm i
            pnpm run -r --filter @bring-it/cli --filter @bring-it/npm build
